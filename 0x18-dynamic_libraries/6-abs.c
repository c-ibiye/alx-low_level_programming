#include "main.h"
#include <stdio.h>

/**
 * _abs - computes the absolute value of an integer
 * @n: The character to compute
 *
 * Return: always 0
 */

int _abs(int n)
{
if (n < 0)
{
	n = -n;
}
	return (n);
}
